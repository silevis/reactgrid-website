---
posttype: "docs"
title: Column reordering
metaTitle: "metaTitle"
metaDescription: "metaDescription"
---

## Column reordering

The following method accepts an array of indexes of selected columns and a target index in which these columns should be placed after releasing the mouse button. Then return reordered columns.

```tsx
private reorderColumns(colIdxs: number[], targetIdx: number): Column[] {
	const fields = [...this.state.fields];
	const movedColumns: Column[] = fields.filter((_, idx) => colIdxs.includes(idx));
	const otherColumns: Column[] = fields.filter((_, idx) => !colIdxs.includes(idx));
	const direction = targetIdx> colIdxs[0] ? 'right' : 'left';
	if (direction === 'right')
		targetIdx = targetIdx - colIdxs.length + 1;
	otherColumns.splice(destination, 0, ...movedColumns);
	return otherColumns;
}
```
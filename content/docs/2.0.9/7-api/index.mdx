---
posttype: "docs"
title: Public API
metaTitle: "Public API"
metaDescription: "metaDescription"
---

ReactGrid API provides following interfaces and types:

## Interfaces

- [`ReactGridProps`](link)
- [`CellChange`](link)
- [`Column`](link)
- [`Row`](link)
- [`Cell`](link)
- [`CellTemplate`](link)
- [`CellLocation`](link)
- [`MenuOption`](link)
- [`CellStyle`](link)

## Types

- [`Id`](link)
- [`Compatible`](link)
- [`Uncertain`](link)
- [`UncertainCompatible`](link)
- [`DropPosition`](link)

## Functions

Keycodes:

- [`isAlphaNumericKey(keyCode: number): boolean`](link)

Used in creating custom cell templates. Checks if `keycode` is alfanumeric.

- [`inNumericKey(keyCode: number): boolean`](link)

Used in creating custom cell templates. Checks if `keycode` is between 0 and 9 (also on numpad).

- [`isNumpadNumericKey(keyCode: number): boolean`](link)

Used in creating custom cell templates. Checks if `keycode` is between 0 and 9 on numpad.

- [`isAllowedOnNumberTypingKey(keyCode: number): boolean`](link)

Used in creating custom cell templates. Checks if `keycode` is allowed on while typing 
numbers (for chars like comma, dash, period).

- [`isNavigationKey(keyCode: number): boolean`](link)

Used in creating custom cell templates. Checks if `keycode` is allowed for navigation 
(arrows, end, home, backspace, delete keys).

Helpers:

Returns cell property value. Used to create compatible cell.

```tsx
getCellProperty = <TCell extends Cell, TKey extends keyof TCell>(
    uncertainCell: Uncertain<TCell>,
    propName: TKey,
    expectedType: 'string' | 'number' | 'boolean' | 'undefined' | 'function' | 'object' | string
)
```

## Enums

`keyCodes` enum costains set of key codes + `POINTER = 1` to marks pointer event (ReactGrid addon). 
Used in cell templates.
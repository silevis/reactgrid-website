---
posttype: "docs"
title: ReactGrid & ReactGridProps
metaTitle: "ReactGridProps interface"
metaDescription: "metaDescription"
---

ReactGrid component has two essential properties which you must pass in props to render - `columns` and `rows`.

## Definition

```ts
interface ReactGridProps {
    readonly columns: Column[];
    readonly rows: Row<Cell>[];
    readonly customCellTemplates?: CellTemplates;
    readonly focusLocation?: CellLocation;
    readonly initialFocusLocation?: CellLocation;
    readonly highlights?: Highlight[];
    readonly stickyTopRows?: number;
    readonly stickyBottomRows?: number;
    readonly stickyLeftColumns?: number;
    readonly stickyRightColumns?: number;
    readonly disableFillHandle?: boolean;
    readonly disableRangeSelection?: boolean;
    readonly enableRowSelection?: boolean;
    readonly enableColumnSelection?: boolean;
    readonly disableFloatingCellEditor?: boolean;

    readonly onCellsChanged?: (cellChanges: CellChange[]) => void;
    readonly onFocusLocationChanging?: (location: CellLocation) => boolean;
    readonly onFocusLocationChanged?: (location: CellLocation) => void;
    readonly onColumnResized?: (columnId: Id, width: number) => void;
    readonly canReorderRows?: (targetRowId: Id, rowIds: Id[], dropPosition: DropPosition) => boolean;
    readonly canReorderColumns?: (targetColumnId: Id, columnIds: Id[], dropPosition: DropPosition) => boolean;
    readonly onRowsReordered?: (targetRowId: Id, rowIds: Id[], dropPosition: DropPosition) => void;
    readonly onColumnsReordered?: (targetColumnId: Id, columnIds: Id[], dropPosition: DropPosition) => void;
    readonly onContextMenu?: (selectedRowIds: Id[], selectedColIds: Id[], selectionMode: SelectionMode, menuOptions: MenuOption[]) => MenuOption[];
}
```

## Properties

Name                            | Type                             | Description
------------------------        | ---------------------------      | -------------
`columns`                       | [`Columns`](link)`[]`            | Column's data
`rows`                          | [`Rows<Cell>`](link)`[]`         | Row's data
`customCellTemplates?`          | [`CellTemplates`](link)          | User is able to create cell template for his own needs
`focusLocation?`                | [`CellLocation`](link)           | Focus position (managed constantly by outer app)
`initialFocusLocation?`         | [`CellLocation`](link)           | Initial position of focus
`highlights?`                   | [`Highlight`](link)`[]`          | Array of highlight positions
`stickyTopRows?`                | `number`                         | Amount of rows which are frozen on the top
`stickyBottomRows?`             | `number`                         | Amount of rows which are frozen on the bottom
`stickyLeftColumns?`            | `number`                         | Amount of columns which are frozen on the left side
`stickyRightColumns?`           | `number`                         | Amount of columns which are frozen on the right side
`disableFillHandle?`            | `boolean`                        | Set `true` to disable cell fill feature (defaultly `false`)
`disableRangeSelection?`        | `boolean`                        | Set `true` to disable selection feature (defaultly `false`)
`enableRowSelection?`           | `boolean`                        | Set `true` to enable row selection feature (defaultly `false`)
`enableColumnSelection?`        | `boolean`                        | Set `true` to enable column selection feature (defaultly `false`)
`disableFloatingCellEditor?`    | `boolean`                        | Set `true` to disable fixed position of cell editor (defaultly `false`)

## Callbacks

Name                        | Type  | Description  |
------------------------    | ----- | ------------ |
`onCellsChanged?`           | `(cellChanges: `[`CellChange`](link)`[]) => void;` | Called when cell property (e.g. value) was changed. See example [implementation](link). |
`onFocusLocationChanging?`  | `(location: `[`CellLocation`](link)`) => boolean;` | Called when trying to change focus location. Return `false` to prevent position changing. See example [implementation](link). |
`onFocusLocationChanged?`   | `(location: `[`CellLocation`](link)`) => void;` | Focus position has been changed. See example [implementation](link). |
`onColumnResized?`          | `(columnId: `[`Id`](link)`, width: number) => void;` | Callback called when column resize action was ended. See example [implementation](link). |
`canReorderRows?`           | `(targetRowId: `[`Id`](link)`, rowIds: `[`Id`](link)`[], dropPosition: `[`DropPosition`](link)`) => boolean;` | Allow or not to change specyfic rows order. See example [implementation](link). | 
`canReorderColumns?`        | `(targetColumnId: `[`Id`](link)`, columnIds: `[`Id`](link)`[], dropPosition: `[`DropPosition`](link)`) => boolean;`| Allow or not to change specific columns order. See example [implementation](link). |
`onRowsReordered?`          | `(targetRowId: `[`Id`](link)`, rowIds: `[`Id`](link)`[], dropPosition: `[`DropPosition`](link)`) => void;`| Callback called when column row reorder action was ended. See example [implementation](link). |
`onColumnsReordered?`       | `(targetColumnId: `[`Id`](link)`, columnIds: `[`Id`](link)`[], dropPosition: `[`DropPosition`](link)`) => void;`| Callback called when column row reorder action was ended. See example [implementation](link). |
`onContextMenu?`            | `(selectedRowIds: `[`Id`](link)`[], selectedColIds: `[`Id`](link)`[], selectionMode: `[`SelectionMode`](link)` , menuOptions: `[`MenuOption`](link)`[]) => `[`MenuOption`](link)`[];` | Called when user opens context menu inside grid, used to compose own menu options. See example [implementation](link). |
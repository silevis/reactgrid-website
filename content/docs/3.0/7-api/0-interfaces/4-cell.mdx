---
posttype: "docs"
title: Cell
metaTitle: "Cell interface"
metaDescription: "metaDescription"
---

`Cell` interface is a basis for built-in cell templates and your own, it has only one necessary prop - `type`.

## Definition

```ts
interface Cell {
    type: string;
    style?: CellStyle;
    className?: string;
}
```

## Properties

| Properties     | Type                 | Properties description |
| -----------    | ---------            | ---------- |
| `type`         | `string`             | Name of cell type, must be unique |
| `style?`       | [`CellStyle`](link)  | Allowed style properties contained in [`CellStyle`](link) interface |
| `className?`   | [`string`](link)     | Additional CSS classes |
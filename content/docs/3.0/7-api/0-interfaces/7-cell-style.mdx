---
posttype: "docs"
title: CellStyle
metaTitle: "CellStyle interface"
metaDescription: "metaDescription"
---

`CellStyle` interface prevents passing unwanted CSS properties that could broke down grid rendering.

## Definition

```ts
export interface CellStyle {
    readonly color?: string;
    readonly background?: string;
    readonly overflow?: string;
    readonly paddingLeft?: string;
}
```

## Properties

| Property name     | Type      |  Property description         |
| -----------       | -------   | -------------------------     |
| `color?`          | `string`  | CSS `color` property          |
| `background?`     | `string`  | CSS `background` property     |
| `overflow?`       | `string`  | CSS `overflow` property       |
| `paddingLeft?`    | `string`  | CSS `padding-left` property   |

---
posttype: "docs"
title: Selection mode
metaTitle: "Selection mode"
metaDescription: "metaDescription"
---

ReactGrid public API contains three available types which can be use in selection. It is `row`, `column` or `range`.

#### Definition

```ts
type SelectionMode = 'row' | 'column' | 'range';
```


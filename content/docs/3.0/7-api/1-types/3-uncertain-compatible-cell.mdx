---
posttype: "docs"
title: UncertainCompatible
metaTitle: "UncertainCompatible type"
metaDescription: "metaDescription"
---

`UncertainCompatible` is a cell type that is compatible between other cell types 
that can be instances of various cell types (e.g. `DataCell` and `TimeCell`).

#### Definition

```ts
type UncertainCompatible<TCell extends Cell> = Uncertain<TCell> & {
    text: string;
    value: number;
}
```